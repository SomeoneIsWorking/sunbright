build/
build-*/
port/build*/
generated/
generated-*/
*.o
*.so
*.a
*.d
compile_commands.json
.cache/
mario_candidates.txt

# transient logs / runtime junk
fm*.log
sb*.log
ms*.log
*.lock
mario_candidates.txt

# Local scratch workspace (core dumps, logs, extracted DOL, frames)
scratch/

# Local machine config (ROM path etc.) and ROM/disc images — never commit (machine-specific / copyrighted)
.env
*.rvz
*.iso
*.gcm
rom.rvz

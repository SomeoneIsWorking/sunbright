---
id: I039
kind: instrument
status: trusted
created: 2026-08-30
---

## Instrument

Shared semantic SDL GPU readback and bounded-runtime summary

## Validated by

The production client encoded an empty frame and measured exactly zero non-clear pixels, then encoded a planted picture through the same path and measured non-clear pixels plus a different hash; duplicate consumption was visibly refused. Guarded recomp and decomp title runs then produced non-clear live samples with completed submissions and no kernel GPU fault. Coverage is only collected J2DPicture commands inside the current screen scope: it does not measure text, windows, fills, direct picture callers, 3D, mip chains, particles, lights, effects, appearance correctness, or cross-runtime parity.

## Known failure modes

(none recorded yet)

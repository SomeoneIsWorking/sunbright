# Copy to .env (gitignored) and set your local paths. Sourced by run.sh / capture.sh / repl.sh,
# and read by the binary via getenv("SUNBRIGHT_ROM").

# Absolute path to the Super Mario Sunshine ROM (RVZ/ISO/GCM). Never commit the ROM itself.
SUNBRIGHT_ROM="/path/to/Super Mario Sunshine (USA).rvz"
